spring:
  application:
    name: skillnest

  # Database Configuration - PRODUCTION (Azure VM)
  datasource:
    url: jdbc:postgresql://4.193.192.105:5432/skillnest_db?currentSchema=public&options=-c%20TimeZone=Asia/Ho_Chi_Minh
    username: skillnest
    password: Se184190!
    driver-class-name: org.postgresql.Driver

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
        jdbc:
          time_zone: Asia/Ho_Chi_Minh

# JWT Configuration
jwt:
  secret: skillnest-super-secret-key-for-jwt-token-generation-minimum-256-bits
  expiration: 86400000

# Server Configuration
server:
  port: 8080

# Logging
logging:
  level:
    com.exe202.skillnest: INFO
    org.springframework.security: WARN

